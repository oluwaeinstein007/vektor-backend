apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-tile-server.fullname" . }}
  labels:
    {{- include "map-tile-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "map-tile-server.selectorLabels" . | nindent 4 }}
