map-tile-server deployed as {{ include "map-tile-server.fullname" . }} in {{ .Release.Namespace }}.

kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "map-tile-server.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
Health check: GET /healthz
