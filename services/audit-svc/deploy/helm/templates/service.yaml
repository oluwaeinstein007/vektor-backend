apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-svc.fullname" . }}
  labels:
    {{- include "audit-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "audit-svc.selectorLabels" . | nindent 4 }}
