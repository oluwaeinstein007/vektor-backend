apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert-svc.fullname" . }}
  labels:
    {{- include "alert-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert-svc.selectorLabels" . | nindent 4 }}
