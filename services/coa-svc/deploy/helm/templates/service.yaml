apiVersion: v1
kind: Service
metadata:
  name: {{ include "coa-svc.fullname" . }}
  labels:
    {{- include "coa-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coa-svc.selectorLabels" . | nindent 4 }}
