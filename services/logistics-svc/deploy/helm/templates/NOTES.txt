logistics-svc deployed as {{ include "logistics-svc.fullname" . }} in {{ .Release.Namespace }}.

kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "logistics-svc.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
Health check: GET /healthz

Requires a Secret named "{{ .Values.existingSecret }}" (keys: DATABASE_URL, ROUTING_DATABASE_URL) to already exist in this namespace (this chart does not create it).
