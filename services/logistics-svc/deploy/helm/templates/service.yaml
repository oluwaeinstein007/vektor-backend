apiVersion: v1
kind: Service
metadata:
  name: {{ include "logistics-svc.fullname" . }}
  labels:
    {{- include "logistics-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logistics-svc.selectorLabels" . | nindent 4 }}
