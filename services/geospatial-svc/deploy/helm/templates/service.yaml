apiVersion: v1
kind: Service
metadata:
  name: {{ include "geospatial-svc.fullname" . }}
  labels:
    {{- include "geospatial-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geospatial-svc.selectorLabels" . | nindent 4 }}
