{
  "name": "@vektor/reporting-svc",
  "version": "0.1.0",
  "private": true,
  "description": "SVC-020: PDF (Puppeteer)/PPTX (pptxgenjs) situation reports, cron-scheduled dispatch, raw CSV/GeoJSON export (§9.7, Epic 7).",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/tests/*.test.js",
    "start": "node dist/src/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@fastify/swagger": "^9.4.0",
    "@fastify/swagger-ui": "^5.1.0",
    "@vektor/auth": "workspace:*",
    "@vektor/db": "workspace:*",
    "@vektor/shared": "workspace:*",
    "cron-parser": "^4.9.0",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.2.0",
    "fastify-type-provider-zod": "^4.0.2",
    "nodemailer": "^9.0.6",
    "pino": "^9.6.0",
    "pptxgenjs": "^3.12.0",
    "puppeteer-core": "^23.11.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.7.2"
  }
}
