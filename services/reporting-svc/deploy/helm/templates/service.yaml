apiVersion: v1
kind: Service
metadata:
  name: {{ include "reporting-svc.fullname" . }}
  labels:
    {{- include "reporting-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reporting-svc.selectorLabels" . | nindent 4 }}
