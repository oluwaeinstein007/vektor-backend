apiVersion: v1
kind: Service
metadata:
  name: {{ include "cv-inference-svc.fullname" . }}
  labels:
    {{- include "cv-inference-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cv-inference-svc.selectorLabels" . | nindent 4 }}
