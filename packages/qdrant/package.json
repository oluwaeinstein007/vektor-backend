{
  "name": "@vektor/qdrant",
  "version": "0.1.0",
  "private": true,
  "description": "ML-008: BGE-M3 embeddings (@xenova/transformers) + Qdrant doctrine store. RAG context retrieval for coa-svc (ML-009). See VEKTOR-PRD.md REQ-3.6, REQ-4.4.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.test.json && node --test dist-tests/tests/*.test.js",
    "clean": "rm -rf dist dist-tests"
  },
  "dependencies": {
    "@qdrant/js-client-rest": "^1.19.0",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
